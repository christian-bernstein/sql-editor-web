/**
 * Used for links with icons, matching their context, like github, twitter, plain link, linkedin, discord etc.
 *
 * () www.xyz.com
 */
export type ContextualLink = {
    context: string,
    link: string
}
